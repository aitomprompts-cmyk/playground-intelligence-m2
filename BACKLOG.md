# 📝 BACKLOG — ส่งต่อ Module 3

> ผลทดสอบล่าสุดผ่านครบ 5 / 5 บนเว็บออนไลน์ (ดู [`test-results.md`](test-results.md)) · รายการด้านล่างคือสิ่งที่ยังไม่เสร็จ ข้อจำกัดที่รู้อยู่ และฟีเจอร์ที่เกินขอบเขต Module 2

## 🔑 1. ย้ายคีย์ AI ไปไว้ฝั่งที่ผู้ใช้แตะไม่ได้ (ทำก่อน)

- [ ] ตอนนี้คีย์ OpenRouter อยู่ในไฟล์ `js/config.js` ในเครื่อง (`.gitignore` กันไว้ ไม่อยู่ใน repo) · ถ้าเอาไฟล์นี้ขึ้นเว็บ ใครเปิดเว็บก็อ่านคีย์ได้ จึงตั้ง `firebase.json` **ไม่ให้ไฟล์นี้ขึ้น Hosting**
- ผลคือ **ปุ่ม AI เสนอประเภทเหตุการณ์ใช้ได้เฉพาะตอนเปิดในเครื่อง (`npm run dev`)** บนเว็บออนไลน์ปุ่มจะบอกว่ายังไม่ได้ตั้งค่าคีย์ และให้เลือกประเภทเอง
- ทางแก้: ให้ Cloud Functions เป็นคนเรียก OpenRouter แทนหน้าเว็บ แล้วเก็บคีย์ใน Secret Manager

## 🧩 2. ฟีเจอร์จาก Feature List ของ Module 1 ที่ยังไม่ได้ทำ

| # | รายการ | ทำไมยังไม่ทำ |
|---|---|---|
| 1 | AI วิเคราะห์วิดีโอจากกล้อง CCTV แล้วสร้างแจ้งเตือนเอง | ต้องต่ออุปกรณ์จริง · Module 2 ใช้ผู้ดูแลระบบกรอกมือแทน |
| 2 | ส่งแจ้งเตือนเข้า LINE OA พร้อมปุ่ม "รับทราบ–กำลังไป" | ระบบภายนอก ไม่อยู่ในหลักสูตร Module 2 |
| 3 | สถานะ `แจ้งเตือนผิด` (false alarm) แยกจาก `ปิดเคส` | คุมสถานะไว้ 3 ตัวตามขอบเขตที่พอดี |
| 4 | Escalation ไปครูที่อยู่ใกล้จุดเกิดเหตุที่สุดเมื่อไม่มีคนรับทราบ | ต้องใช้ตำแหน่งครูจาก Live Map |
| 5 | Live Map แสดงตำแหน่งครูและโซน | เป็นหน้าแสดงผล ไม่ใช่ CRUD |
| 6 | แยกโซนเป็นโฟลเดอร์ `zones` พร้อมตารางเวรรายวัน | คุมโฟลเดอร์ประกอบไว้ 2 ตัว · ตอนนี้ใช้ `zoneName` เป็นข้อความ |
| 7 | ระดับความรุนแรง (severity) ของแจ้งเตือน | ไม่จำเป็นต่อวงจร สร้าง → รับทราบ → ปิดเคส |
| 8 | Dashboard ผู้บริหาร + สรุปรายวันด้วย AI | กราฟและแดชบอร์ดเป็นเนื้อหา Module 3 |
| 9 | ค้นหา กรอง และแบ่งหน้ารายการ | ตอนนี้จำกัดที่ 100 รายการ |

## 🧪 3. สิ่งที่เทสต์จับได้แต่ยังไม่ได้แก้

- **ไม่มี** — บั๊ก 2 จุดที่เทสต์จับได้แก้แล้วทั้งหมด (รายละเอียดใน `test-results.md`)

## ⚠️ 4. ข้อจำกัดที่รู้อยู่

- [ ] **บทบาทผู้ดูแลระบบกำหนดด้วยรายชื่ออีเมลในกฎ** (`roleForEmail` ใน `firestore.rules`) เพราะยังไม่มีหน้าจัดการผู้ใช้ · ของจริงควรให้ผู้ดูแลตั้งบทบาทผ่าน Firebase Console หรือ Admin SDK แล้วเอารายชื่อออกจากกฎ
- [ ] ชื่อที่จดซ้ำ (`teacherName` `patternName` `authorName`) กฎยังไม่ได้ตรวจว่าตรงกับไฟล์ต้นทาง
- [ ] แจ้งเตือนที่เทสต์สร้าง (ขึ้นต้นด้วย `[เทสต์]`) ค้างอยู่ในฐานข้อมูล — ควรมีขั้นเก็บกวาดหลังเทสต์
- [ ] เพิ่มเทสต์: ผู้ดูแลระบบลบแจ้งเตือน · บันทึกการตอบสนอง · ครูเวรเปิดหน้าสร้างแจ้งเตือนไม่ได้ · ปุ่ม AI เมื่อเรียกไม่สำเร็จแล้วระบบไม่ค้าง
- [ ] ฐานข้อมูลทดลองอยู่ในโปรเจกต์ `zone-alert-pi-m2` แยกจากระบบจริงของ Playground Intelligence (Supabase) — ข้อมูลทั้งหมดเป็นข้อมูลสมมติ
