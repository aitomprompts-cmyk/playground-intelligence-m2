// ─────────────────────────────────────────────────────────
// js/config.example.js — ตัวอย่างไฟล์ตั้งค่าคีย์ AI (OpenRouter)
// ─────────────────────────────────────────────────────────
//
// วิธีใช้:
// 1) คัดลอกไฟล์นี้เป็น "js/config.js" (ชื่อไฟล์ต้องเป็น config.js เป๊ะ)
// 2) กรอก apiKey ของคุณเองจาก https://openrouter.ai/keys ลงในช่อง apiKey ด้านล่าง
// 3) เปิดระบบด้วย "npm run dev" (รันในเครื่องของคุณเอง) ปุ่ม AI จะใช้งานได้ทันที
//
// ⚠️ js/config.js อยู่ใน .gitignore แล้ว — จะไม่ถูก commit ขึ้น Git
//    และถูกกันไว้ไม่ให้ deploy ขึ้น Firebase Hosting โดยเจตนา
//    เพื่อไม่ให้คีย์จริงหลุดออกไปอยู่บนเว็บสาธารณะ
//
// ผลที่ตามมา: ปุ่ม "ให้ AI ช่วยเสนอประเภทเหตุการณ์" ในหน้าสร้างแจ้งเตือน
// จะใช้งานได้ "เฉพาะตอนเปิดระบบในเครื่องที่มีไฟล์ js/config.js เท่านั้น"
// บนเว็บที่ deploy จริง ปุ่มนี้จะขึ้นข้อความแจ้งว่ายังไม่ได้ตั้งค่าคีย์ — เป็นพฤติกรรมที่ตั้งใจ
// ผู้ใช้ยังเลือกประเภทเหตุการณ์เองได้ตามปกติเสมอ
// การย้ายคีย์ไปไว้ฝั่งเซิร์ฟเวอร์ (เพื่อให้ใช้ได้บนเว็บจริงด้วย) อยู่ใน BACKLOG ของโครงงาน (ยังไม่ทำในรอบนี้)

export const aiConfig = {
  // ใส่คีย์ OpenRouter ของคุณ
  apiKey: "ใส่คีย์ OpenRouter ของคุณ",

  // ชื่อโมเดลที่ต้องการใช้ (ปล่อยว่างได้ — js/ai.js จะใช้ค่าเริ่มต้นให้อัตโนมัติ)
  model: "google/gemini-2.5-flash-lite",
};
