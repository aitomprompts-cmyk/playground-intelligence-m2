# 🎯 SCOPE — ส่วนของโครงงานที่เลือกทำใน Module 2

**โครงงาน:** Playground Intelligence (Capstone ADT-RAISE Batch 2)
**ส่วนที่เลือก:** Zone Alert — แจ้งเตือนเหตุการณ์ในโซน ที่ครูเวรต้องกดรับทราบ
**เทียบแนวโครงงานในคู่มือข้อ 3️⃣:** 📡 IoT / เซ็นเซอร์ → ใบแจ้งเตือนที่ต้องมีคนกดรับทราบ

> ไม่ทำทั้งระบบ ส่วนวิเคราะห์วิดีโอด้วย AI, LINE และแผนที่สด อยู่ใน [`BACKLOG.md`](BACKLOG.md)
> ตามคู่มือข้อ 4️⃣ ส่วนที่ต้องต่อกับอุปกรณ์จริง (กล้อง CCTV) ให้ทำหน้ากรอกข้อมูลด้วยมือแทนไปก่อน

---

## 2️⃣ ประโยคเดียว

> ระบบของฉันเก็บ **แจ้งเตือนเหตุการณ์ในโซน** ที่ **ผู้ดูแลระบบ** สร้างขึ้น (แทนระบบ AI ในช่วง Module 2)
> แต่ละรายการเลือก **ประเภทเหตุการณ์** ได้ และมีสถานะ **รอรับทราบ → รับทราบแล้ว → ปิดเคส**
> โดย **ครูเวรประจำโซน** เป็นคนกดเปลี่ยน

## 1️⃣ เช็กครบ 6 อย่าง

| # | LeaveEasy | Zone Alert ของเรา | ครบ |
|---|---|---|---|
| 1 | 📁 `leaveRequests` รายการหลัก | 📁 `alerts` แจ้งเตือนที่เพิ่มเข้ามาทุกวันช่วงพักกลางวัน | ✅ |
| 2 | ✏️ `requesterId` เป็นของใคร | ✏️ `teacherId` ครูเวรที่รับผิดชอบแจ้งเตือนนี้ | ✅ |
| 3 | ✏️ `status` สถานะที่เปลี่ยนได้ | ✏️ `status` รอรับทราบ → รับทราบแล้ว → ปิดเคส | ✅ |
| 4 | 📁 `leaveTypes` ประเภทให้เลือก | 📁 `alertPatterns` ประเภทเหตุการณ์ | ✅ |
| 5 | 📁 `approvals` โฟลเดอร์ย่อย | 📁 `responses` บันทึกการตอบสนองของครูต่อแจ้งเตือนนั้น | ✅ |
| 6 | 📝 `reason` ข้อความยาว | 📝 `description` รายละเอียดเหตุการณ์ที่เห็นในกล้อง | ✅ |

**ผล: ครบ 6 ข้อ**

## 6️⃣ ตารางขอบเขต (ใช้ทั้ง 4 สัปดาห์)

| | 🔧 LeaveEasy | 🛡️ Zone Alert ของเรา |
|---|---|---|
| 📁 โฟลเดอร์หลัก | `leaveRequests` | `alerts` |
| 📁 โฟลเดอร์ประเภท | `leaveTypes` | `alertPatterns` |
| 📁 โฟลเดอร์ย่อย | `approvals` | `responses` |
| ✏️ ช่องบอกว่าเป็นของใคร | `requesterId` · `requesterName` | `teacherId` · `teacherName` |
| 🔀 สถานะทั้งหมด | รอพิจารณา · อนุมัติ · ไม่อนุมัติ | รอรับทราบ · รับทราบแล้ว · ปิดเคส |
| 👤 คนที่สร้างรายการ | พนักงาน | ผู้ดูแลระบบ (แทนระบบ AI ในช่วง Module 2) |
| 👤 คนที่เปลี่ยนสถานะ | หัวหน้า | ครูเวรประจำโซน |
| 📝 ช่องข้อความยาวที่ AI จะอ่าน | `reason` | `description` |
| 🤖 งานที่ AI ช่วย (สัปดาห์ที่ 8) | จัดประเภทการลาให้อัตโนมัติ | อ่านรายละเอียดเหตุการณ์ แล้วเสนอประเภทเหตุการณ์ให้อัตโนมัติ |

## 🔁 เทียบกับที่ทำในคาบ

| ในคาบทำกับ LeaveEasy | ของเรา |
|---|---|
| 📁 `leaveRequests` = โฟลเดอร์หลัก | 📁 `alerts` |
| 📁 `users` = คนที่ใช้ระบบ | 📁 `users` ครูเวร · ผู้ดูแลระบบ |
| 📁 `leaveTypes` = ประเภทที่เลือกได้ | 📁 `alertPatterns` เข้าพื้นที่ต้องห้าม · เล่นเสี่ยงอันตราย · รวมกลุ่มผิดปกติ |
| 📁 `approvals` = โฟลเดอร์ย่อยของใบลา | 📁 `responses` บันทึกการตอบสนองของครู |
| สถานะ รอพิจารณา → อนุมัติ / ไม่อนุมัติ | รอรับทราบ → รับทราบแล้ว → ปิดเคส |
| `requesterName` จดชื่อซ้ำไว้ | `teacherName` · `patternName` · `authorName` |

## 5️⃣ ขอบเขตที่พอดี

| | จำนวน | ของเรา |
|---|---|---|
| 📁 โฟลเดอร์หลัก | 1 | `alerts` |
| 📁 โฟลเดอร์ประกอบ | 2 | `users` · `alertPatterns` |
| 📁 โฟลเดอร์ย่อย | 1 | `responses` |
| 🖥️ หน้าจอ | 3–4 | รายการแจ้งเตือน · สร้างแจ้งเตือน · รายละเอียด + ปุ่มเปลี่ยนสถานะ · (จัดการประเภทเหตุการณ์) |
| 👤 บทบาทผู้ใช้ | 2 | ผู้ดูแลระบบ · ครูเวร |
| 🔀 สถานะ | 3 | รอรับทราบ · รับทราบแล้ว · ปิดเคส |

## 🔐 กติกาการเห็นข้อมูล (ใช้ตอนสัปดาห์ที่ 7–8)

- ครูเวรเห็นและเปลี่ยนสถานะได้ **เฉพาะแจ้งเตือนที่ `teacherId` เป็นของตัวเอง**
- ผู้ดูแลระบบเห็นแจ้งเตือนทั้งหมด และเป็นคนสร้างแจ้งเตือน
