# 📝 โครงสร้างข้อมูล Firestore — Zone Alert

> ร่างนี้ใช้เป็นต้นแบบให้ **ลอกลงกระดาษด้วยมือ** ตามส่วน B ของการบ้าน
> กระดาษไม่ต้องส่ง แต่ต้องทำเอง เพราะการวาดด้วยมือทำให้เห็นว่าอะไรขาด

## B1 — กติกา 4 ข้อ

1. 📁 **Collection** = โฟลเดอร์ · 📄 **Document** = ไฟล์ · ✏️ **Field** = ช่องข้อมูลในไฟล์
2. Firestore **ไม่มี JOIN**
3. จึงต้อง **จดชื่อซ้ำไว้ในไฟล์เลย** (denormalize) เช่นเก็บ `teacherName` ไว้ในแจ้งเตือนตรง ๆ
4. ของที่เป็นของรายการนั้นโดยเฉพาะ ให้เป็น **โฟลเดอร์ย่อย** (subcollection) เช่นบันทึกการตอบสนองของครู

## B2 — โฟลเดอร์ 3 + โฟลเดอร์ย่อย 1

🔴 = ช่องที่จดชื่อซ้ำ ให้ **วงกลมสีแดง** บนกระดาษ แล้วเขียนข้าง ๆ ว่า "จดซ้ำเพราะไม่มี JOIN"

```
📁 users            → name · email · role
📁 alertPatterns    → name
📁 alerts           → title · description · zoneName · status · createdAt
                      teacherId · teacherName 🔴
                      patternId · patternName 🔴
   └ 📁 responses   → authorId · authorName 🔴 · message · createdAt
```

| ช่องที่จดซ้ำ | จดจากไหน |
|---|---|
| 🔴 `alerts.teacherName` | `users/{teacherId}.name` |
| 🔴 `alerts.patternName` | `alertPatterns/{patternId}.name` |
| 🔴 `responses.authorName` | `users/{authorId}.name` |

**ค่าที่ใช้ได้**

- `users.role` → `duty_teacher` (ครูเวร) · `admin` (ผู้ดูแลระบบ)
- `alerts.status` → `รอรับทราบ` · `รับทราบแล้ว` · `ปิดเคส`
- `createdAt` → ข้อความรูปแบบ `YYYY-MM-DD HH:mm` เช่น `2026-09-11 11:33` จึงเรียงใหม่ไปเก่าได้ตรง
- `zoneName` → ข้อความธรรมดา เช่น `โซน 4 หลังอาคาร 3` ยังไม่แยกเป็นโฟลเดอร์ เพื่อคุมขอบเขตไว้ที่ 3 โฟลเดอร์

## ข้อมูลตัวอย่าง (ชื่อสมมติทั้งหมด)

| id | ข้อมูล |
|---|---|
| `users/u001` | สมชาย ใจดี · duty_teacher · ดูแลโซน 1, 4, 5 |
| `users/u002` | สมหญิง รักงาน · duty_teacher · ดูแลโซน 2, 3 |
| `users/u003` | สมศรี ตั้งใจ · admin |
| `alertPatterns/pt001` | เข้าพื้นที่ต้องห้าม |
| `alertPatterns/pt002` | เล่นเสี่ยงอันตราย |
| `alertPatterns/pt003` | รวมกลุ่มผิดปกติ |
| `alerts/al001` – `al005` | 3 รอรับทราบ · 1 รับทราบแล้ว · 1 ปิดเคส |
| `alerts/al001/responses/rs001` | บันทึกของสมชาย |
| `alerts/al002/responses/rs002` | บันทึกของสมหญิง |

## B3 — ตอบคำถาม 2 ข้อ

**① ถ้าครูสมชายเปลี่ยนชื่อ ต้องไปแก้กี่ที่?**

ในข้อมูลตัวอย่างต้องแก้ **5 ที่**

- `users/u001` 1 ที่ ต้นทางของชื่อ
- `alerts` ทุกใบที่ `teacherId = u001` 3 ที่ คือ al001 · al004 · al005
- `responses` ทุกรายการที่ `authorId = u001` 1 ที่ คือ al001/responses/rs001

นี่คือราคาของการจดชื่อซ้ำ ยิ่งมีแจ้งเตือนมาก ยิ่งต้องแก้หลายที่
แต่ชื่อครูเปลี่ยนน้อยมาก ส่วนหน้ารายการถูกเปิดทุกวัน การอ่านเร็วโดยไม่ต้อง JOIN จึงคุ้มกว่า

**② ทำไม `responses` จึงเป็นโฟลเดอร์ย่อย ไม่ใช่โฟลเดอร์แยก?**

- บันทึกการตอบสนองเป็นของแจ้งเตือนใบนั้นโดยเฉพาะ แยกออกมาเดี่ยว ๆ ก็ไม่มีความหมาย
- หน้ารายละเอียดอ่านบันทึกทีละแจ้งเตือนเสมอ เปิด `alerts/{id}/responses` ได้ตรง ๆ ไม่ต้องค้นทั้งฐาน
- เขียนกฎสิทธิ์ผูกกับแจ้งเตือนแม่ได้ เช่น เฉพาะครูเวรเจ้าของแจ้งเตือนเท่านั้นที่เขียนบันทึกได้
- บันทึกเพิ่มได้ไม่จำกัด โดยไฟล์แจ้งเตือนไม่ใหญ่ขึ้น
