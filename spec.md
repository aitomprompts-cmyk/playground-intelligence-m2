# ข้อกำหนดระบบ (Spec) — 🛡️ Zone Alert · Playground Intelligence

> **ใบสั่งงานของระบบ Zone Alert** — รวม [`SCOPE.md`](SCOPE.md) · [`DATA-STRUCTURE.md`](DATA-STRUCTURE.md) และสิ่งที่ทำมาตั้งแต่สัปดาห์ที่ 6 ไว้ในไฟล์เดียว
> ⚠️ **กติกาสำหรับ AI ที่อ่านเอกสารนี้:** ทำเฉพาะสิ่งที่เขียนไว้ **ห้ามเพิ่มฟีเจอร์ที่ไม่ได้ระบุ** — ของที่อยากได้เพิ่มให้เขียนเป็นข้อเสนอไว้ใน `BACKLOG.md` แทน

---

## 0. ข้อกำหนดทางเทคนิค (บังคับ ห้ามเปลี่ยนเอง)

| หัวข้อ | ข้อกำหนด |
|---|---|
| หน้าเว็บ | HTML · CSS · JavaScript ธรรมดา **ห้ามใช้ framework** · หนึ่งไฟล์ `.html` ต่อหนึ่งหน้าจอ · หน้าตาใน `css/` · การทำงานใน `js/` |
| ฐานข้อมูล | Firestore โปรเจกต์ `zone-alert-pi-m2` (ที่ตั้ง `asia-southeast1`) |
| ล็อกอิน | Firebase Authentication แบบอีเมล/รหัสผ่าน |
| ยามเฝ้าข้อมูล | Security Rules ในไฟล์ `firestore.rules` |
| นำขึ้นออนไลน์ | Firebase Hosting |
| AI | OpenRouter · คีย์อยู่ใน `js/config.js` ที่ `.gitignore` กันไว้ และไม่ขึ้นเว็บออนไลน์ |
| เซิร์ฟเวอร์ | ไม่มีเซิร์ฟเวอร์ที่เขียนเอง — หน้าเว็บคุยกับ Firestore ตรง |
| หน้าตา | ใช้ `css/style.css` เดิม (สีจาก design system ของ Playground Intelligence) · ภาษาไทยทั้งหมด · ใช้บนมือถือได้ |

---

## 1. ระบบนี้แก้ปัญหาอะไร ให้ใคร

**Playground Intelligence** คือระบบ AI วิเคราะห์ภาพจากกล้อง CCTV เดิมของโรงเรียน แล้วแจ้งเตือนครูเวรเมื่อเกิดเหตุในโซนที่รับผิดชอบ
Module 2 ทำเฉพาะส่วน **Zone Alert** — ใบแจ้งเตือนเหตุการณ์ในโซน ที่ครูเวรต้องกดรับทราบ

- ทุกวันนี้ครูเวรช่วงพักกลางวันดูแลหลายโซนพร้อมกัน เหตุในมุมอับจึงถูกเห็นช้า และไม่มีบันทึกว่าใครรับเรื่องเมื่อไร
- Zone Alert เก็บแจ้งเตือนทุกใบไว้ที่เดียว มอบให้ครูเวรประจำโซน ครูกดรับทราบและปิดเคส พร้อมบันทึกการตอบสนอง
- ช่วง Module 2 **ผู้ดูแลระบบสร้างแจ้งเตือนด้วยมือแทนระบบ AI วิเคราะห์ภาพ** (ส่วนต่อกล้องจริงอยู่ใน BACKLOG)

**เส้นทางหลัก:** ผู้ดูแลระบบสร้างแจ้งเตือน → ครูเวรเห็นในรายการ → กดรับทราบ → ไปถึงจุดเกิดเหตุ → ปิดเคส

---

## 2. ผู้ใช้ 2 บทบาท

| บทบาท | ค่าในช่อง `role` | ทำอะไรได้ | ทำอะไรไม่ได้ |
|---|---|---|---|
| 🧑‍🏫 **ครูเวร** | `duty_teacher` | เห็นเฉพาะแจ้งเตือนที่ `teacherId` เป็นของตัวเอง · เปลี่ยนสถานะแจ้งเตือนของตัวเอง · เขียนบันทึกการตอบสนองในแจ้งเตือนของตัวเอง | เห็นแจ้งเตือนของครูคนอื่น · สร้างหรือลบแจ้งเตือน · แก้ประเภทเหตุการณ์ |
| 🛠️ **ผู้ดูแลระบบ** | `admin` | เห็นแจ้งเตือนทั้งหมด · สร้างแจ้งเตือนและมอบให้ครูเวร · ลบแจ้งเตือนที่ยัง `รอรับทราบ` · เขียนบันทึกในทุกแจ้งเตือน · เพิ่ม แก้ ลบ ประเภทเหตุการณ์ | เปลี่ยนสถานะแทนครูเวร |

- สมัครสมาชิกใหม่ได้บทบาท `duty_teacher` เสมอ · ตั้งตัวเองเป็น `admin` จากหน้าเว็บไม่ได้
- บัญชีผู้ดูแลระบบของรอบนี้กำหนดด้วยรายชื่ออีเมลในกฎ (`tester.admin@example.com`) เพราะยังไม่มีหน้าจัดการผู้ใช้

---

## 3. User Story และเกณฑ์การยอมรับ

### ZA-01 — ดูรายการแจ้งเตือน
**ในฐานะ** ครูเวร **ฉันต้องการ** เห็นแจ้งเตือนของฉันเป็นตาราง **เพื่อ** รู้ว่ามีเหตุไหนรอฉันอยู่

- [ ] ตารางมีคอลัมน์: เวลา · โซน · เหตุการณ์ · ประเภท · สถานะ · ครูเวรที่รับผิดชอบ
- [ ] เรียงจากใหม่ไปเก่าตาม `createdAt`
- [ ] สถานะเป็นป้ายสี (รอรับทราบ = เหลือง · รับทราบแล้ว = ฟ้า · ปิดเคส = เขียว)
- [ ] กดแถวแล้วไปหน้ารายละเอียด
- [ ] ครูเวรเห็นเฉพาะของตัวเอง · ผู้ดูแลระบบเห็นทั้งหมด
- [ ] ไม่มีรายการเลย ขึ้นว่า "ยังไม่มีแจ้งเตือนในระบบ"

### ZA-02 — สร้างแจ้งเตือน (ผู้ดูแลระบบ)
- [ ] ฟอร์มมีช่อง: หัวข้อเหตุการณ์ · รายละเอียด (หลายบรรทัด) · โซน · ประเภทเหตุการณ์ (เลื่อนลงจาก `alertPatterns`) · ครูเวรที่รับผิดชอบ (เลื่อนลงจากผู้ใช้ที่เป็น `duty_teacher`)
- [ ] **ทุกช่องบังคับกรอก** — กรอกไม่ครบต้องไม่บันทึก และขึ้นข้อความบอกว่าขาดช่องไหน
- [ ] บันทึกแล้วสถานะเริ่มที่ `รอรับทราบ` เสมอ มี `createdAt` อัตโนมัติ แล้วพากลับหน้ารายการ
- [ ] มีปุ่มยกเลิก · ครูเวรเปิดหน้านี้ได้แต่ขึ้นว่าสำหรับผู้ดูแลระบบเท่านั้น

### ZA-03 — ดูรายละเอียดแจ้งเตือน
- [ ] แสดงครบทุกช่อง: หัวข้อ · รายละเอียด · โซน · ประเภท · ครูเวร · สถานะ · เวลาที่แจ้ง
- [ ] ด้านล่างมีบันทึกการตอบสนอง เรียงเก่าไปใหม่ · แต่ละรายการมี ชื่อผู้เขียน · ข้อความ · เวลา
- [ ] เปิดแจ้งเตือนที่ไม่มีสิทธิ์ ต้องไม่เห็นข้อมูล และขึ้นข้อความว่าไม่มีสิทธิ์

### ZA-04 — เปลี่ยนสถานะ (ครูเวรเจ้าของแจ้งเตือน)
- [ ] ใบที่ `รอรับทราบ` มีปุ่ม **รับทราบ** · ใบที่ `รับทราบแล้ว` มีปุ่ม **ปิดเคส** · ใบที่ `ปิดเคส` ไม่มีปุ่ม
- [ ] กดแล้วสถานะบนหน้าจอและในฐานข้อมูลเปลี่ยน · **แก้เฉพาะช่อง `status`**
- [ ] ปุ่มขึ้นเฉพาะครูเวรเจ้าของแจ้งเตือน

### ZA-05 — บันทึกการตอบสนอง
- [ ] กล่องพิมพ์ + ปุ่มส่ง · เก็บใน **โฟลเดอร์ย่อย `responses`** ของแจ้งเตือนนั้น
- [ ] ส่งข้อความว่างไม่ได้ ต้องขึ้นข้อความเตือน

### ZA-06 — จัดการประเภทเหตุการณ์ (ผู้ดูแลระบบ)
- [ ] ตารางประเภททั้งหมด · ช่องเพิ่ม · ปุ่มแก้ไขและลบในแต่ละแถว
- [ ] ครูเวรเห็นเป็นรายการอ่านอย่างเดียว

### ZA-07 — ลบแจ้งเตือน (ผู้ดูแลระบบ)
- [ ] ลบได้เฉพาะใบที่ยัง `รอรับทราบ` · ต้องถามยืนยันก่อน · ลบแล้วกลับหน้ารายการ

### ZA-08 — ล็อกอิน และข้อมูลไม่รั่ว
- [ ] หน้าสมัครสมาชิก + เข้าสู่ระบบ · สมัครแล้วมีไฟล์ใน `users` บทบาท `duty_teacher`
- [ ] **ไม่ล็อกอิน อ่านข้อมูลไม่ได้เลย**
- [ ] **ครูเวรคนหนึ่ง เปิดแจ้งเตือนของครูเวรอีกคนไม่ได้** ทั้งจากหน้าเว็บและจากการถามฐานข้อมูลตรง
- [ ] กฎครอบโฟลเดอร์ย่อย `responses` ด้วย

### ZA-09 — ปุ่ม AI เสนอประเภทเหตุการณ์
- [ ] หน้าสร้างแจ้งเตือนมีปุ่ม **"ให้ AI ช่วยเสนอประเภทเหตุการณ์"** อ่านจากช่องรายละเอียด
- [ ] ผลต้องเป็นประเภทที่มีอยู่จริงเท่านั้น ถ้าไม่ตรงให้บอกว่าเสนอให้ไม่ได้ และไม่เปลี่ยนค่าเดิม
- [ ] มีป้าย **"ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน"** · ระหว่างรอกดซ้ำไม่ได้
- [ ] ล้มเหลวหรือเกิน 15 วินาที ระบบต้องไม่ค้าง บันทึกได้ตามปกติ · ผู้ใช้เลือกเองได้เสมอ
- [ ] ส่งเฉพาะข้อความรายละเอียดเหตุการณ์ไปให้ AI **ห้ามส่งชื่อครูหรือนักเรียน**

---

## 4. หน้าจอ

| ไฟล์ | หน้าจอ | ใครใช้ |
|---|---|---|
| `index.html` | หน้าแรก — อธิบายระบบ บทบาท และสถานะ | ทุกคน (ไม่ต้องล็อกอิน · ไม่มีข้อมูลจากฐาน) |
| `login.html` | เข้าสู่ระบบ / สมัครสมาชิก | ทุกคน |
| `alerts.html` | รายการแจ้งเตือน | ครูเวร · ผู้ดูแลระบบ |
| `new-alert.html` | สร้างแจ้งเตือน + ปุ่ม AI | ผู้ดูแลระบบ |
| `alert-detail.html` | รายละเอียด · ปุ่มเปลี่ยนสถานะ · บันทึกการตอบสนอง · ลบ | ครูเวรเจ้าของ · ผู้ดูแลระบบ |
| `alert-patterns.html` | จัดการประเภทเหตุการณ์ | ผู้ดูแลระบบ (ครูเวรอ่านอย่างเดียว) |

---

## 5. โครงสร้างข้อมูลบน Firestore

```
📁 users/{uid}            { name, email, role }
📁 alertPatterns/{id}     { name }
📁 alerts/{id}            { title, description, zoneName, status, createdAt,
                            teacherId, teacherName, patternId, patternName }
   └ 📁 responses/{id}    { authorId, authorName, message, createdAt }
```

| ช่อง | ชนิด | คำอธิบาย |
|---|---|---|
| `users.role` | ข้อความ | `duty_teacher` · `admin` |
| `alerts.title` | ข้อความ | หัวข้อเหตุการณ์ |
| `alerts.description` | ข้อความ | รายละเอียดที่เห็นในกล้อง (ช่องที่ AI อ่าน) |
| `alerts.zoneName` | ข้อความ | เช่น `โซน 4 หลังอาคาร 3` |
| `alerts.status` | ข้อความ | 3 ค่าตามหัวข้อ 6 เท่านั้น |
| `alerts.createdAt` · `responses.createdAt` | ข้อความ | `YYYY-MM-DD HH:mm` เรียงตามตัวอักษรได้ตรงกับเวลา |
| `alerts.teacherId` / 🔴 `teacherName` | ข้อความ | ครูเวรที่รับผิดชอบ · ชื่อจดซ้ำจาก `users` |
| `alerts.patternId` / 🔴 `patternName` | ข้อความ | ประเภทเหตุการณ์ · ชื่อจดซ้ำจาก `alertPatterns` |
| `responses.authorId` / 🔴 `authorName` | ข้อความ | ผู้เขียนบันทึก · ชื่อจดซ้ำจาก `users` |

🔴 = จดซ้ำเพราะ Firestore ไม่มี JOIN (เหตุผลเต็มอยู่ใน `DATA-STRUCTURE.md`) · ⚠️ ชื่อช่องต้องสะกดตามตารางนี้เป๊ะ

---

## 6. สถานะ

```
รอรับทราบ  →  รับทราบแล้ว  →  ปิดเคส
```

- แจ้งเตือนใหม่เริ่มที่ `รอรับทราบ` เสมอ ไม่ให้ผู้ใช้เลือกเอง
- เปลี่ยนได้ตามลูกศรทีละขั้นเท่านั้น ห้ามข้ามขั้น ห้ามย้อนกลับ · `ปิดเคส` คือปลายทาง
- เปลี่ยนได้เฉพาะครูเวรเจ้าของแจ้งเตือน และแก้เฉพาะช่อง `status`

---

## 7. ข้อมูลตัวอย่าง

ประเภทเหตุการณ์ `pt001` เข้าพื้นที่ต้องห้าม · `pt002` เล่นเสี่ยงอันตราย · `pt003` รวมกลุ่มผิดปกติ
แจ้งเตือน 5 ใบตาม `DATA-STRUCTURE.md` (3 รอรับทราบ · 1 รับทราบแล้ว · 1 ปิดเคส) มอบให้ครูเวรทดสอบ 2 คน
**ชื่อทั้งหมดเป็นชื่อสมมติ อีเมลเป็น `@example.com`** · ห้ามใส่ข้อมูลจริงของครูหรือนักเรียน

---

## 8. สิ่งที่ไม่ทำใน Module นี้ (อยู่ใน `BACKLOG.md`)

- ❌ AI วิเคราะห์วิดีโอจากกล้อง CCTV · ❌ ส่งแจ้งเตือนเข้า LINE / อีเมล / SMS
- ❌ สถานะที่ 4 เช่น `แจ้งเตือนผิด` · ❌ ระดับความรุนแรง · ❌ Escalation ไปครูคนอื่น
- ❌ Live Map · ❌ โฟลเดอร์ `zones` และตารางเวร · ❌ แดชบอร์ดและกราฟ · ❌ ค้นหา/กรอง/แบ่งหน้า
- ❌ แก้ไขเนื้อหาแจ้งเตือนหลังสร้าง · ❌ แก้ไขหรือลบบันทึกการตอบสนอง · ❌ บทบาทที่ 3
- ❌ framework · เซิร์ฟเวอร์ของตัวเอง · Cloud Functions · Firebase Storage
